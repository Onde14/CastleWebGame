

enum GameSize {
    width = 1000,
    height = 1000,
}

export enum CastleSize {
    width = 250,
    height = 250,
}

export enum SoldierConfig {
    width = 10,
    height = 10,
    color = "black",
    radius = 15,
}

export enum RoadSize {
    width = 10,
}

export enum VillageSize {
    width = 150,
    height = 150,
}