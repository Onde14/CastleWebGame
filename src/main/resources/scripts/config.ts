

enum GameSize {
    width = 1000,
    height = 1000,
}

export enum CastleConfig {
    width = 50,
    height = 50,
    color = "gray",
    ownerColorWidth = 25,
    ownerColorHeight = 25,
}

export enum SoldierConfig {
    width = 10,
    height = 10,
    color = "black",
    radius = 15,
    ownerColorRadius = 8,
}

export enum RoadSize {
    width = 10,
}

export enum VillageSize {
    width = 150,
    height = 150,
}