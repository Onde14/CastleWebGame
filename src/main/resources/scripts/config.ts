

export enum CastleSize {
    width = 250,
    height = 250,
}

export enum SoldierSize {
    width = 10,
    height = 10,
}

export enum RoadSize {
    width = 10,
}

export enum VillageSize {
    width = 150,
    height = 150,
}